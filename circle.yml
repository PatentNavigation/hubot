machine:
  pre:
    - mkdir ~/.yarn-cache
  node:
    version: 6.9.1
dependencies:
  pre:
    - sudo apt-get update
  cache_directories:
    - ~/.yarn-cache
  override:
    # Install Yarn
    - sudo env "PATH=$PATH" npm install -g yarn
    - yarn install
test:
  override:
    - yarn test
